# InstaDM for Windows — Source-Derived Behavior Specification

Status: SKELETON — populated during Phase 1 (M1) from the Obsidian notes, the
local macOS source, and the public repository. No private note text is copied;
behaviors are distilled with evidence types only.

## Format

For each behavior:

- **Behavior/requirement name**
- **Product intent**
- **Evidence:** note | local code | public code | README | changelog | git history
- **macOS implementation summary**
- **Hard-won edge cases / regressions**
- **Windows preservation strategy**
- **Confidence:** high | medium | low
- **Status:** preserve exactly | adapt natively | defer | intentionally omit

(Behavior entries are added in M1.)
